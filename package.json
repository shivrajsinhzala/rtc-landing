{
  "name": "rtc-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-state RTC bus tracking/info landing pages. One domain, one state per subdirectory — see README for why.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && npm run verify",
    "preview": "astro preview",
    "verify": "node scripts/verify-build.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.2.10"
  }
}
